#include <bits/stdc++.h>
#define mod 998244353

using namespace std;
using ll = long long;

int main(){
    int N, M;
    cin >> N >> M;
    ll A, B, C, D, E, F;
    cin >> A >> B >> C >> D >> E >> F;
    vector<int> X(M), Y(M);
    for (int i = 0; i < M; i++){
        cin >> X[i] >> Y[i];
    }


    return 0;
}