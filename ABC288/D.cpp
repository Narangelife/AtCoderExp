#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main(){
    int N, K, Q;
    cin >> N >> K;
    vector<ll> A(N);
    for (int i = 0; i < N; i++){
        cin >> A[i];
    }
    cin >> Q;
    for (int i = 0; i < Q; i++){
        int l, r;
        cin >> l >> r;

    }
    return 0;
}