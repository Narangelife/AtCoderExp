#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main(){
    int N;
    cin >> N;
    vector<int> T(N), X(N), A(N);
    for (int i = 0; i < N; i++){
        cin >> T[i] >> X[i] >> A[i];
    }


    return 0;
}