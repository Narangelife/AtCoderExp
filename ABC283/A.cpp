#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main(){
    int A, B;
    cin >> A >> B;

    ll answer = (ll)pow((double)A, (double)B);
    cout << answer << endl;
    return 0;
}