#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main(){
    int N, Q, X;
    cin >> N >> Q >> X;
    vector<int> W(N);
    for (int i = 0; i < N; i++){
        cin >> W[i];
    }

    for (int i = 0; i < Q; i++){
        long K;
        cin >> K;


    }


    return 0;
}